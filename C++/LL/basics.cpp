#include<bits/stdc++.h>
using namespace std;

// class Node{
// public: 
// int val;
//  Node * next;
// };

int main(){
    list<int>l;
    cout<<sizeof(l);
    l.push_back(1);
    l.push_back(1);
    l.push_back(1);
    l.push_back(1);
      cout<<sizeof(l);
    // cout<<l.back();
    // Node a;
    // Node b;
    // Node c;

    // // giving value to our list
    // a.val = 10;
    // b.val= 20;
    // c.val = 30;

    // // creation of linked list
    // a.next = &b;
    // b.next= &c;
    // c.next = NULL;

    return 0;
}